services:
  - type: web
    name: intellishelf-store
    runtime: python
    buildCommand: ""
    startCommand: uvicorn store.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
    plan: free
    autoDeploy: true
    pythonVersion: 3.10
